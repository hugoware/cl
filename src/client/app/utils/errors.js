
import _ from 'lodash';

/** tries to extract an error from the argument
 * @param {any} err the object to resolve
 * @returns {string} the error key
 */
export function resolveError(err) {
	// if (_.isError(err)) return { err: err.message };
	// if (_.isString(err)) return { err };
	// if (_.isObject(err)) return err;
	// return { err: 'unkonw'}
}