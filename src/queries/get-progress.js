
const PROGRESS = { 
	web: 10,
	code: 10,
};


/** returns access for  */
export async function getAccess(profile = { }) {


	// const web = isNaN(profile.web) ? 1 : profile.web;
	// const code = isNaN(profile.code) ? 1 : profile.code;

	// const web = _.clamp((profile.web || 0) + 1, 1, PROGRESS.web);
	// const web = _.clamp((profile.web || 0) + 1, 1, PROGRESS.web);

}